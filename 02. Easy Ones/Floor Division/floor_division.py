# Prompt the user to enter the first number, then convert the input string to an integer
num1 = int(input('Enter the first number: '))

# Prompt the user to enter the second number, then convert the input string to an integer
num2 = int(input('Enter the second number: '))

# Perform integer (floor) division on num1 by num2 and store the result in the variable 'result'
result = num1 // num2

# Output the result of the division to the console
print(result)
