import random  # Import the random module to access functions for generating random numbers

# Generate a random integer between 0 and 10 (inclusive)
random_num = random.randint(0, 10)

# Print the generated random number to the console
print(random_num)
